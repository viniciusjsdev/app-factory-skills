interface:
  display_name: "App Factory Infra Orchestrator"
  short_description: "Create Docker, Supabase and Vercel infrastructure."
  brand_color: "#2563EB"
  default_prompt: "Use $app-factory-infra-orchestrator to create or normalize the App Factory infrastructure for this monorepo."

policy:
  allow_implicit_invocation: true

