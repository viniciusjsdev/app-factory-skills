# Backend Testing

Record project commands and required DTO, service, repository, API, permission, security, migration, integration/browser, and architecture checks.

Trace every manifest invariant and endpoint ID to implementation files, exact `test_*` names, and command results. Required deferred evidence is a blocker or correction, not a passed gate.
