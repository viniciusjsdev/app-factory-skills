interface:
  display_name: "App Factory Frontend Builder"
  short_description: "Build secure React MVPs for Sites workflows"
  default_prompt: "Use $app-factory-frontend-builder as the mandatory stack and architecture contract while @sites builds, previews, and privately publishes this product."
