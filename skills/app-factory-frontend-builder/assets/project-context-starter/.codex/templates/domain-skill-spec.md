# Domain Skill Specification

- Proposed skill name:
- Domain capability:
- Triggering user requests:
- Requests that must not trigger it:
- Required domain references:
- Deterministic scripts or validations:
- Expected output or project change:

Create the resulting skill under `.agents/skills/<skill-name>/` with `SKILL.md`, optional `references/`, `scripts/`, `assets/`, and `agents/openai.yaml`.
