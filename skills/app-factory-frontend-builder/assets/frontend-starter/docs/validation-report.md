# Validation Report

Record the date, environment, npm and Node versions, lockfile/signature/audit results, commands executed, browser paths checked, passed validations, failures, and remaining gaps.
