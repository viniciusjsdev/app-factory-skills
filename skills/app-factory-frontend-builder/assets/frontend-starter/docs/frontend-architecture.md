# Frontend Architecture

Replace this starter note with the implemented provider order, error boundary, lazy routes, feature ownership, service and repository boundaries, mock/API selection, storage adapters, environment variables, and intentional exceptions.
