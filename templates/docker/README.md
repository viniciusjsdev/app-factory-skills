# Docker Template

Use this folder for Docker and Compose starter conventions.

Expected generated files usually include:

- root `docker-compose.yml`
- optional `docker-compose.override.yml`
- optional `docker-compose.prod.yml`
- service healthcheck notes
- local development commands
